import 'package:flutter/material.dart';

class ErrorAnswerTest extends StatefulWidget {
  const ErrorAnswerTest({Key? key}) : super(key: key);

  @override
  State<ErrorAnswerTest> createState() => _AnswerPageState();
}

class _AnswerPageState extends State<ErrorAnswerTest> {
  final List<String> titles = [
    "Нэр томьёоны тодорхойлолт",
    "Механикжсан тээврийн хэрэгслийн ангилал",
    "Замын хөдөлгөөний зохион байгуулалт",
    "Замын тэмдэг",
    "Тэмдэглэгээ",
    "Замын хөдөлгөөний дохио",
    "Тээврийн хэрэгслийн гэрэл, дохио",
    "Жолоочийн үүрэг",
    "Согтууруулах ундааны нөлөө",
    "Аюулгүй байдал",
    "Зогсоолын дүрэм",
    "Дохио өгөх журам",
    "Эргэх үйлдэл",
    "Тээврийн хэрэгслийн бүрэн бүтэн байдал",
    "Тээврийн хэрэгслийн хурд",
    "Шархтныг тээвэрлэх журам",
    "Жолооны эрх хасах үндэслэл",
    "Нийтийн тээврийн хэрэгсэл",
    "Зөвшөөрөгдөх ачаалал",
    "Суудлын бүс",
    "Гэрлийн дохио",
    "Тусгай дуут дохио",
    "Осолтой тохиолдолд",
    "Тээврийн хэрэгслийн ачаа"
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      appBar: AppBar(
        backgroundColor: Colors.white,
        title: const Text(
          "Алдсан тестүүд",
          style: TextStyle(color: Colors.black, fontWeight: FontWeight.bold),
        ),
        centerTitle: true,
      ),
      body: ListView.builder(
        padding: const EdgeInsets.all(16),
        itemCount: titles.length,
        itemBuilder: (context, index) {
          return Card(
            elevation: 3,
            margin: const EdgeInsets.only(bottom: 12),
            shape: RoundedRectangleBorder(
              borderRadius: BorderRadius.circular(12),
            ),
            child: ListTile(
              leading: CircleAvatar(
                backgroundColor: Colors.blueAccent,
                child: Text(
                  "${index + 1}",
                  style: const TextStyle(color: Colors.white),
                ),
              ),
              title: Text(
                titles[index],
                style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 14),
              ),
              trailing: const Icon(Icons.arrow_forward_ios, size: 16),
              onTap: () {
                // TODO: Navigate to error test details of that sub-topic
              },
            ),
          );
        },
      ),
    );
  }
}
